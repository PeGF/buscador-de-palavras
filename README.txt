Para compilar o programa, é necessário usar o gcc e compilar o arquivo main.c
Para rodar o programa, o primeiro argumento deverá ser o nome do programa, seguido do nome do arquivo txt que será lido e, por último, o índice (lista ou árvore)
Ou seja, pode seguir à risca as orientações do enunciado.